[
  {
    "ns": "client",
    "fn": "resolve",
    "call": "crate.resolve(query)",
    "http": "GET",
    "ep": "/api/v2/resolve",
    "auth": "key",
    "ret": "IdentityResolution",
    "retry": true,
    "idem": true,
    "sig": "resolve(query: string | ResolveQuery, opts?: RequestOptions): Promise<IdentityResolution>",
    "desc": "Resolve any identifier to a canonical IdentityResolution. Identity is the hard problem in music data — the same artist is a Bandcamp subdomain, a Discogs id, an MBID and five spellings. This collapses any of them to crate's single canonical `cluster_id`, plus every other place that artist lives online. It's the first call in most workflows, because almost everything else is keyed on `cluster_id`. Accepts a bare string (inferred: URL → `url`, `discogs:`/`mbid:` → locator, 64-hex → `cluster`, else → `q`) or an explicit one-of object. A 200 with `cluster_id: null` is an honest gap, not an error.",
    "example": "const id = await crate.resolve('https://fourtet.bandcamp.com');\n// → { cluster_id: '9f2c…', display: 'Four Tet', resolved_from: 'url',\n//     locators: { discogs: 1234, bandcamp: ['fourtet'], … } }\nawait crate.resolve({ discogs: 1234 }); // or an explicit one-of",
    "throws": [
      [
        "exactly_one_of",
        "if zero/multiple identifiers are given."
      ],
      [
        "CrateAPIError",
        "on a non-2xx response."
      ]
    ]
  },
  {
    "ns": "client",
    "fn": "artist",
    "call": "crate.artist(key)",
    "http": "GET",
    "ep": "/api/v2/artist/{key}",
    "auth": "key",
    "ret": "ArtistDossierContract",
    "retry": true,
    "idem": true,
    "sig": "artist(key: string, opts?: RequestOptions): Promise<ArtistDossierContract>",
    "desc": "Fetch an artist dossier in one call — the shortcut when you already mean a specific artist. Hand it almost anything (a name, slug, 64-hex `cluster_id`, or `discogs:`/`mbid:` locator): a direct key hits `/artist/{key}`, while a locator or bare numeric id is resolved first. The dossier is full by default; pass `{ fields }` to trim it. An unresolved locator throws CrateNotFoundError — use artistOrNull to receive `null`. In v2 (cluster-first), release/master detail attaches here as the `discography` dimension and Bandcamp standing as `bandcamp_emergence` / `bandcamp_tastemaker` — there are no standalone `master`/`bandcamp` methods.",
    "example": "const a = await crate.artist('Four Tet');                 // name → dossier\nawait crate.artist('discogs:1234');                       // locator → resolve → dossier\nawait crate.artist('Four Tet', { fields: ['discography'] }); // trim to one facet",
    "throws": [
      [
        "not_found",
        "when a locator/numeric id resolves to no cluster."
      ],
      [
        "invalid_fields",
        ")."
      ]
    ]
  },
  {
    "ns": "client",
    "fn": "artistBandcampRelease",
    "call": "crate.artistBandcampRelease(key, item)",
    "http": "GET",
    "ep": "/api/v2/artist/{key}/bandcamp/{item}",
    "auth": "key",
    "ret": "ArtistBandcampReleaseResponse",
    "retry": true,
    "idem": true,
    "sig": "artistBandcampRelease(key: string, item: string, opts?: RequestOptions): Promise<ArtistBandcampReleaseResponse>",
    "desc": "Fetch one Bandcamp release addressed *under its artist* — the full tracklist (with per-track `duration_s`), fetchable artwork URLs, label, tags, and economics. The cluster-attached per-release grain: list `item` ids from an artist dossier's `bandcamp_releases` facet, then hand `(key, item)` here for the detail. Cluster-first integrity: if the release is unknown OR filed under a *different* artist, the response is the honest gap `{ present: false, note }` — never another artist's data, never a 404. Branch on `present`.",
    "example": "const r = await crate.artistBandcampRelease(clusterId, '2783508421');\nif (r.present) console.log(r.release.title, r.release.tracks.length);",
    "throws": [
      [
        "CrateAPIError",
        "on a non-2xx response."
      ]
    ]
  },
  {
    "ns": "client",
    "fn": "artistOrNull",
    "call": "crate.artistOrNull(key)",
    "http": "GET",
    "ep": "/api/v2/artist/{key}",
    "auth": "key",
    "ret": "ArtistDossierContract | null",
    "retry": true,
    "idem": true,
    "sig": "artistOrNull(key: string, opts?: RequestOptions): Promise<ArtistDossierContract | null>",
    "desc": "Like artist, but returns `null` for the honest-gap case (a locator or numeric id that resolves to no cluster) instead of throwing. crate is honest about absence — reach for this in pipelines and agent loops where 'not found' is a normal branch, not an exception to catch.",
    "example": "const a = await crate.artistOrNull('discogs:999999'); // → null if unresolved\nif (a) console.log(a.display);",
    "throws": [
      [
        "CrateAPIError",
        "on a non-2xx response."
      ]
    ]
  },
  {
    "ns": "client",
    "fn": "label",
    "call": "crate.label(key)",
    "http": "GET",
    "ep": "/api/v2/label/{key}",
    "auth": "key",
    "ret": "LabelDossierContract",
    "retry": true,
    "idem": true,
    "sig": "label(key: string, opts?: RequestOptions): Promise<LabelDossierContract>",
    "desc": "Fetch a label dossier in one call (cluster-first) by `cluster_id` or slug — the label peer of artist, promoted to a first-class resource in v2.",
    "example": "const l = await crate.label('warp-records');",
    "throws": [
      [
        "empty_key",
        "on an empty/whitespace key."
      ],
      [
        "CrateAPIError",
        "on a non-2xx response."
      ]
    ]
  },
  {
    "ns": "client",
    "fn": "search",
    "call": "crate.search(params)",
    "http": "GET",
    "ep": "/api/v2/search",
    "auth": "key",
    "ret": "SearchResponse",
    "retry": true,
    "idem": true,
    "sig": "search(params?: SearchParams, opts?: RequestOptions): Promise<SearchResponse>",
    "desc": "Faceted discovery across the whole catalogue. Filter by genre, style, format, country, label and year range — each multi-value facet combines with AND or OR via its `*_mode` sibling. The response also tells you whether the total count is exact or estimated from a sample.",
    "example": "const hits = await crate.search({ genre: ['idm', 'ambient'], genre_mode: 'or', year_from: 2000, limit: 20 });\n// → hits.results: ResultRow[]; hits.pagination.total_results / total_results_mode",
    "throws": [
      [
        "CrateAPIError",
        "on a non-2xx response."
      ]
    ]
  },
  {
    "ns": "client",
    "fn": "breakouts",
    "call": "crate.breakouts()",
    "http": "GET",
    "ep": "/api/v2/breakouts",
    "auth": "key",
    "ret": "BreakoutsResponse",
    "retry": true,
    "idem": true,
    "sig": "breakouts(opts?: RequestOptions): Promise<BreakoutsResponse>",
    "desc": "The emerging-artists breakouts index — artists breaking out now, backed by corroborating evidence (press, bookings) rather than a single noisy signal. Each item carries an emergence tier, a score, and how it was corroborated.",
    "example": "const b = await crate.breakouts();\nb.items.forEach((i) => console.log(i.name, i.emergenceTier, i.corroboration));",
    "throws": [
      [
        "CrateAPIError",
        "on a non-2xx response."
      ]
    ]
  },
  {
    "ns": "client",
    "fn": "index",
    "call": "crate.index()",
    "http": "GET",
    "ep": "/api/v2",
    "auth": "anon",
    "ret": "ApiRootIndex",
    "retry": true,
    "idem": true,
    "sig": "index(opts?: RequestOptions): Promise<ApiRootIndex>",
    "desc": "The self-describing API root — a machine-readable map of every resource plus a 'cold start' recipe, task recipes, and a runtime error catalogue (v2). The one **keyless** endpoint, so a new integration or an agent can discover what's possible before committing to anything.",
    "example": "const root = await crate.index(); // works without an apiKey\nconsole.log(root.cold_start.problem);\nroot.resources.forEach((r) => console.log(r.name, r.auth));",
    "throws": [
      [
        "CrateAPIError",
        "on a non-2xx response."
      ]
    ]
  },
  {
    "ns": "client",
    "fn": "facets",
    "call": "crate.facets()",
    "http": "GET",
    "ep": "/api/v2/facets",
    "auth": "key",
    "ret": "FacetCounts",
    "retry": true,
    "idem": true,
    "sig": "facets(opts?: RequestOptions): Promise<FacetCounts>",
    "desc": "A precomputed facet snapshot — the available filter values and their counts. Call it before building a search UI (or an agent query) so you filter by values that actually return results. **Key-gated.**",
    "example": "const facets = await crate.facets(); // requires apiKey",
    "throws": [
      [
        "api_key_required",
        "if constructed without an apiKey."
      ],
      [
        "CrateAPIError",
        "on a non-2xx response (401/402 if the key lacks access)."
      ]
    ]
  },
  {
    "ns": "dossier",
    "fn": "artist",
    "call": "crate.dossier.artist(slug)",
    "http": "GET",
    "ep": "/api/v2/dossier/artist/{slug}",
    "auth": "key",
    "ret": "ArtistDossierContract",
    "retry": true,
    "idem": true,
    "sig": "artist(slug: string, opts?: RequestOptions): Promise<ArtistDossierContract>",
    "desc": "The deepest grain crate offers: an artist profile from ~24 independent facets (emergence, live demand, tastemaker support, journalism, discography, the Bandcamp facets…), each section present-with-signals or honestly marked absent. crate's flagship. Accepts `{ fields }` to trim the dossier (default = full).",
    "example": "const d = await crate.dossier.artist('four-tet');\n// → d.emergence.signals?.emergenceTier, d.discography.signals?.entries, …",
    "throws": []
  },
  {
    "ns": "dossier",
    "fn": "label",
    "call": "crate.dossier.label(slug)",
    "http": "GET",
    "ep": "/api/v2/dossier/label/{slug}",
    "auth": "key",
    "ret": "LabelDossierContract",
    "retry": true,
    "idem": true,
    "sig": "label(slug: string, opts?: RequestOptions): Promise<LabelDossierContract>",
    "desc": "The label-grain dossier by slug (cluster-first). Also reachable as the top-level label.",
    "example": "const d = await crate.dossier.label('warp-records');",
    "throws": []
  },
  {
    "ns": "dossier",
    "fn": "festival",
    "call": "crate.dossier.festival(slug)",
    "http": "GET",
    "ep": "/api/v2/dossier/festival/{slug}",
    "auth": "key",
    "ret": "FestivalDossierContract",
    "retry": true,
    "idem": true,
    "sig": "festival(slug: string, opts?: RequestOptions): Promise<FestivalDossierContract>",
    "desc": "The festival-grain dossier by slug (identity + lineup signals) — useful for tying artists to the events that book them.",
    "example": "const d = await crate.dossier.festival('dekmantel');",
    "throws": []
  },
  {
    "ns": "dossier",
    "fn": "manifest",
    "call": "crate.dossier.manifest()",
    "http": "GET",
    "ep": "/api/v2/dossier/manifest",
    "auth": "key",
    "ret": "DossierManifest",
    "retry": true,
    "idem": true,
    "sig": "manifest(opts?: RequestOptions): Promise<DossierManifest>",
    "desc": "A table of contents for the dossier system — which grains exist, which are available, and the contract version you're coding against. In v2, `master` is demoted (it surfaces honestly under `unavailable_grains`).",
    "example": "const m = await crate.dossier.manifest();\nconsole.log(m.contract_version, m.grains, m.unavailable_grains);",
    "throws": []
  },
  {
    "ns": "tastemakers",
    "fn": "tastemakers",
    "call": "crate.tastemakers()",
    "http": "GET",
    "ep": "/api/v2/tastemakers",
    "auth": "key",
    "ret": "TastemakersResponse",
    "retry": true,
    "idem": true,
    "sig": "crate.tastemakers(opts?: RequestOptions): Promise<TastemakersResponse>",
    "desc": "The tastemaker leaderboard — early-moving curators/DJs ranked, with ones-to-watch and early-spinner slices. Leading indicators, not vanity counts.",
    "example": "const t = await crate.tastemakers();\nt.leaderboard.forEach((x) => console.log(x.rank, x.name, x.ownTier));",
    "throws": []
  },
  {
    "ns": "tastemakers",
    "fn": "onesToWatch",
    "call": "crate.tastemakers.onesToWatch()",
    "http": "GET",
    "ep": "/api/v2/tastemakers/ones-to-watch",
    "auth": "key",
    "ret": "OnesToWatchResponse",
    "retry": true,
    "idem": true,
    "sig": "onesToWatch(opts?: RequestOptions): Promise<OnesToWatchResponse>",
    "desc": "Just the 'ones to watch' slice — rising names with emergence and momentum tiers.",
    "example": "const w = await crate.tastemakers.onesToWatch();",
    "throws": []
  },
  {
    "ns": "aura",
    "fn": "aura",
    "call": "crate.aura(params)",
    "http": "GET",
    "ep": "/api/v2/aura",
    "auth": "key",
    "ret": "AuraIndexResponse",
    "retry": true,
    "idem": true,
    "sig": "crate.aura(params?: { limit?: number }, opts?: RequestOptions): Promise<AuraIndexResponse>",
    "desc": "The aura index — artists whose signals are converging across several dimensions at once, strongest first. `state: 'degraded'` = a substrate read failed (items empty, still HTTP 200) — branch on it rather than assuming rows.",
    "example": "const a = await crate.aura({ limit: 100 });\na.items.forEach((x) => console.log(x.display, x.convergence_dim_count));",
    "throws": []
  },
  {
    "ns": "aura",
    "fn": "artist",
    "call": "crate.aura.artist(clusterId)",
    "http": "GET",
    "ep": "/api/v2/aura/{cluster}",
    "auth": "key",
    "ret": "AuraArtistResponse",
    "retry": true,
    "idem": true,
    "sig": "artist(clusterId: string, opts?: RequestOptions): Promise<AuraArtistResponse>",
    "desc": "One artist's aura row by 64-hex `cluster_id`. `present: false` is a normal answer (single-dimension artists are filtered by the universe rule; inactive artists age out of the window) — branch on `present`, don't treat it as an error.",
    "example": "const a = await crate.aura.artist(clusterId);\nif (a.present) console.log(a.break_odds);",
    "throws": []
  },
  {
    "ns": "searchEvents",
    "fn": "observed",
    "call": "crate.searchEvents.observed(body)",
    "http": "POST",
    "ep": "/api/v2/search-events/observed",
    "auth": "beacon",
    "ret": "void",
    "retry": false,
    "idem": false,
    "sig": "observed(body: ObservedBeaconInput, opts: RequestOptions & { beaconToken: string }): Promise<void>",
    "desc": "Report an observed search event (e.g. a cache hit). Returns nothing on success — fire-and-acknowledge telemetry.",
    "example": "await crate.searchEvents.observed(\n  { search_event_id: id, source: 'swr-cache-hit' },\n  { beaconToken },\n);",
    "throws": [
      [
        "beacon_token_required",
        "when `beaconToken` is missing."
      ]
    ]
  },
  {
    "ns": "searchEvents",
    "fn": "refined",
    "call": "crate.searchEvents.refined(body)",
    "http": "POST",
    "ep": "/api/v2/search-events/refined",
    "auth": "beacon",
    "ret": "void",
    "retry": false,
    "idem": false,
    "sig": "refined(body: RefinedBeaconInput, opts: RequestOptions & { beaconToken: string }): Promise<void>",
    "desc": "Report a refined search — which facets changed (from → to) — so crate learns how people narrow results.",
    "example": "await crate.searchEvents.refined(\n  { search_event_id: id, changed_facets: [{ name: 'genre', to: 'idm' }] },\n  { beaconToken },\n);",
    "throws": [
      [
        "beacon_token_required",
        "when `beaconToken` is missing."
      ]
    ]
  }
]
