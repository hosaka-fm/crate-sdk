{
  "name": "@hosaka-fm/crate-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Docs site for @hosaka-fm/crate — a static Astro Starlight shell over the SDK's single-source generation pipeline. Never shipped in the npm package.",
  "scripts": {
    "sync": "node scripts/sync-content.mjs",
    "predev": "npm run sync",
    "prebuild": "npm run sync",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "devDependencies": {
    "@astrojs/starlight": "^0.40.0",
    "astro": "^6.4.8",
    "sharp": "^0.35.2",
    "starlight-llms-txt": "^0.10.0",
    "starlight-openapi": "^0.25.3"
  }
}
