---
title: Guides & recipes
description: Hand-authored guides — authentication, errors, AI agents, recipes, configuration.
---

The guides cover the things the reference can't: how to think about auth tiers, how to branch on
typed errors, how an AI agent should consume the SDK, and copy-paste recipes.

:::note[Scaffold — synced content]
At Phase 1 these pages are **synced from the canonical `docs/*.md`** in the repo root
(getting-started, authentication, errors, ai-agents, recipes, configuration) so there is one
source, not two copies. The sync step writes into this directory at build time and the synced
copies are git-ignored. Until then, see [`docs/`](https://github.com/hosaka-fm/crate-sdk/tree/main/docs).
:::
